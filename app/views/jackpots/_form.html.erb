<%= form_for(@jackpot) do |f| %>
  <% if @jackpot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jackpot.errors.count, "error") %> prohibited this jackpot from being saved:</h2>

      <ul>
      <% @jackpot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :max_entries %><br>
    <%= f.number_field :max_entries %>
  </div> 
  <div class="field">
    <%= f.label :open %><br>
    <%= f.check_box :open %>
  </div> 
  <div class="field">
    <%= f.label :prize %><br>
    <%= f.text_field :prize %>
  </div> 

  <div class="field">
    <%= f.label :draw_date %><br>
    <%= f.date_select :draw_date %>
  </div> 

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
